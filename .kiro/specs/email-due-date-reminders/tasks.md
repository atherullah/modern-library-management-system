# Tasks

## Task List

- [x] 1. Create `utils/reminderService.js` with `sendDueReminders()` logic
- [x] 2. Create `cron/reminderCron.js` with setInterval scheduler
- [x] 3. Add `send_reminders` controller action to `adminController.js`
- [x] 4. Add `POST /admin/reminders/send` route to `adminRoutes.js`
- [x] 5. Add manual trigger button to `views/admin/index.ejs`
- [x] 6. Require `reminderCron.js` in `app.js`
